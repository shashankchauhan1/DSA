// java

class Solution {
    public int countOdds(int low, int high) {
        int cnt = 0;
        int len = high-low+1;

        if((len&1)==0) cnt += len/2;
        else if((low&1)!=0 && (high&1)!=0) cnt += (len/2)+1;
        else cnt += len/2;

        return cnt;
    }
}
