// java

class Solution {
    public boolean isPalindrome(int x) {
        int sign= x < 0 ? -1 : 1;
        int sum=0;
        int n=x;
        while(x>0){
            int rem=x%10;
            sum=sum*10+rem;
            x/=10;
        }
        return sum*sign == n;
    }   
}
