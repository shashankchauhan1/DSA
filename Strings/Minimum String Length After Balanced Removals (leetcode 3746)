// java

class Solution {
    public int minLengthAfterRemovals(String s) {
        int[] freq = new int[2];
        for(int i=0; i<s.length(); i++) freq[s.charAt(i)-'a']++;

        return Math.abs(freq[1]-freq[0]);
    }
}
